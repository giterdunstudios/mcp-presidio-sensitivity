# MCP Presidio Sensitivity Server — dependencies
# Pin minor versions; patch versions resolved at build time.
# Scan with 'pip-audit' before each release.

# MCP SDK — FastMCP interface for tool registration and protocol handling
mcp>=1.0,<2.0

# HTTP framework (MCP SDK requires fastapi; we also use it directly)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# JWT validation — PyJWT with cryptography backend for RS256 (Keycloak)
# python-jose removed: transitive ecdsa dep (CVE-2024-23342), no upstream fix.
# PyJWT is already in the tree via the mcp SDK; pinned here explicitly.
PyJWT[crypto]>=2.8,<3.0

# HTTP client for backend worker calls
httpx>=0.27,<1.0

# Request/response validation
pydantic>=2.6,<3.0
