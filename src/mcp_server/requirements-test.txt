# Test-only dependencies — not installed in production image
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
# httpx is already in requirements.txt; repeated here for explicit test declaration
httpx>=0.27,<1.0
