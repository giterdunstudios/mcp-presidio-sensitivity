# Tech Debt & Operational Debt Backlog

Produced by council audit (2026-03-26). Covers all known tech debt, operational
debt, and governance gaps identified across all personas.

**Last updated:** 2026-03-27 (council review of all Wave 1+2 branches complete; numbering corrected; items #36–#50 added across all reviews)

**Summary:**
- 34 items are self-contained and can be picked up independently
- 12 items are blocked — 4 on Phase 3 planning (#15, #16, #17, #25), 3 on Integration/API Lead activation (#29, #30, #31), 2 on other items in this list (#12 on #11, #23 on #22), 1 on scope definition (#17), 1 on vertical templates council approval (#33), 1 on #11 merge (#42)
- 3 burst/future personas need activation before their items can start: Platform/Cluster Infrastructure Lead (#15, #25, #27), Detection/Data Researcher (#24), Integration/API Lead (#29, #30, #31)

**Cross-persona flag resolutions (2026-03-27):**
- CVE failure policy (#20): warn on HIGH (exit 0), block on CRITICAL (exit 1)
- Credential gate (#6): standalone `scripts/check-credentials.sh`; rebuild.sh calls it in one line; default = dry-run, `--strict` available; backlog item added to review and tighten after 1 month operational data
- Helm version bump (#13): split — Part A = policy doc now (planning/helm-versioning-policy.md); Part B = coordinated 0.1.0→0.2.0 bump after #14+#26 merge
- SBOM cdxgen gate (#22): Security Lead commits BP-001 status → `approved-for-implementation`; concerns → council review
- #31 folded into #1+#2 batch: branch `1-2-31-registry-gc` covers all three items

**Wave assignments:**
- Wave 1 (fully parallel): #3, #4, #8, #9, #11, #18, #19
- Wave 2 (minor contention): #1+#2+#31, #6, #7, #10, #13
- Wave 3 (sequential, rebuild.sh): #5 → #14 → #20
- Wave 4 (lower priority): #21, #22 (gate on Security sign-off), #26, #28
- Wave TBD (council review items): #34, #35, #45, #46 (fix in branch 4-sbom-refresh or standalone); #36 (fix in branch 7-rfc9728-integration-test or standalone); #37, #38 (standalone low-priority); #39, #42 (Wave 1/2 planning); #40 (standalone post-DR-runbook); #41 (Wave TBD doc); #43 (fix in branch 10-k3d-version-check); #44 (fix in branch 6-credential-gate); #47, #48 (fix in branch 1-2-31-registry-gc); #49, #50 (Wave TBD)

**Independent column:** ✅ Yes = self-contained branch, no blocker | ⛔ No = explicitly blocked (reason noted)

---

| # | Item | Why needed | Priority | Independent? | Wave / Branch | Implementation Owner | Validation Owner |
|---|------|-----------|----------|-------------|--------------|---------------------|-----------------|
| 1 | **Registry GC script** (`scripts/registry-gc.sh`) | Unreferenced image layers accumulate on every rebuild — silent disk growth | High | ✅ Yes | Wave 2 / `1-2-31-registry-gc` | Engineering Practices Lead | Technical Implementation Lead |
| 2 | **Registry process documentation** in `scripts/README.md` | No documented policy for when/how to run GC | High | ✅ Yes | Wave 2 / `1-2-31-registry-gc` | Engineering Practices Lead | Product / Scope Lead |
| 3 | **Worker pod restart root cause** investigation | 9 restarts in status.sh — unknown instability going into Phase 3 | High | ✅ Yes | Wave 1 / `3-worker-restart-investigation` | Technical Implementation Lead | Engineering Practices Lead |
| 4 | **SBOM (`bom.json`) refresh post-Phase 2** | auth/ deleted, Istio/Envoy CRDs added — current SBOM doesn't reflect running system | High | ✅ Yes | Wave 1 / `4-sbom-refresh` | Security / Privacy Lead | Technical Implementation Lead |
| 5 | **requirements.lock.txt sync check** in rebuild.sh | Nothing verifies lock file matches requirements.txt — could ship wrong deps silently | High | ✅ Yes | Wave 3 / `5-lockfile-sync-check` (after Wave 2) | Technical Implementation Lead | Engineering Practices Lead |
| 6 | **Hardcoded credential pre-prod gate** | CLIENT_SECRET / ADMIN_PASSWORD have change-in-prod values with no enforcement gate | High | ✅ Yes | Wave 2 / `6-credential-gate` | Security / Privacy Lead | Engineering Practices Lead |
| 7 | **BP-007** Integration test for RFC 9728 discovery chain | auth-test.sh covers enforcement; no test for the full discovery chain a client follows | Medium | ✅ Yes | Wave 2 / `7-rfc9728-integration-test` | Technical Implementation Lead | Security / Privacy Lead |
| 8 | **BP-010** Disaster recovery runbook | No documented recovery path — what breaks, what to run, how long it takes | Medium | ✅ Yes | Wave 1 / `8-dr-runbook` | Engineering Practices Lead | Product / Scope Lead |
| 9 | **BP-011** Verify `setup-local.sh --teardown` leaves no orphaned resources | Teardown/re-run path not validated — orphaned registry volumes possible | Medium | ✅ Yes | Wave 1 / `9-teardown-verification` | Technical Implementation Lead | Engineering Practices Lead |
| 10 | **BP-012** k3d version floor check in `setup-local.sh` | Script checks presence only — silent behaviour changes from k3d version mismatch | Medium | ✅ Yes | Wave 2 / `10-k3d-version-check` | Engineering Practices Lead | Technical Implementation Lead |
| 11 | **BP-013** Document dev/prod parity delta | No formal baseline of what's simplified locally vs. production target | Medium | ✅ Yes | Wave 1 / `11-parity-delta` | Engineering Practices Lead | Product / Scope Lead |
| 12 | **BP-014** Define acceptable parity threshold | No rule for "if X absent locally, you cannot test Y" | Medium | ⛔ No — depends on #11 | Wave 2 (after #11) / `12-parity-threshold` | Engineering Practices Lead | Product / Scope Lead |
| 13 | **Helm chart version bump policy** (Part A: policy doc) | Both charts frozen at 0.1.0 — can't correlate running pod to chart version in audit trail | Medium | ✅ Yes | ~~Wave 2 / `13-helm-version-policy`~~ **COMPLETE** — `planning/helm-versioning-policy.md` committed to main 2026-03-27 | Engineering Practices Lead | Technical Implementation Lead |
| 13b | **Helm chart version bump** (Part B: 0.1.0→0.2.0) | Reflect Phase 2 changes in chart version | Medium | ⛔ No — wait for #14+#26 to merge | Coordinated PR after Wave 3+4 | Engineering Practices Lead | Technical Implementation Lead |
| 14 | **Helm test hooks** | No post-deploy smoke test; Helm native support unused | Medium | ✅ Yes | Wave 3 / `14-helm-test-hooks` (after #13) | Technical Implementation Lead | Engineering Practices Lead |
| 15 | **Istio IngressGateway** | Port-level PERMISSIVE on port 8000 is a workaround — IngressGateway is the correct solution | Medium | ⛔ No — Phase 3 scope definition required first | Phase 3 | Platform / Cluster Infrastructure Lead (reactivate burst) | Security / Privacy Lead + Technical Implementation Lead |
| 16 | **Per-identity rate limiting** (DEC-003 cases 25+29) | Per-pod shared counter — one caller exhausts limit for all | Medium | ⛔ No — requires Redis + global rate limit service | Phase 3 | Technical Implementation Lead | Security / Privacy Lead |
| 17 | **EnvoyFilter replacement** (DEC-005) | Bypasses Istio stable API — breaks silently on any minor Istio upgrade | Medium | ⛔ No — depends on #15 (WWW-Auth) and #16 (rate limit) | Phase 3 | Technical Implementation Lead | Security / Privacy Lead |
| 18 | **Vertical templates — schedule a phase** | Research complete since 2026-03-26; no implementation phase assigned | Medium | ✅ Yes (planning task only) | Wave 1 / `18-19-planning` | Product / Scope Lead | Engineering Practices Lead |
| 19 | **SLO definition** | No p99 latency or error rate targets — Phase 3 observability has no pass/fail criteria | Medium | ✅ Yes (planning task only) | Wave 1 / `18-19-planning` | Product / Scope Lead | Engineering Practices Lead |
| 20 | **Image vulnerability scanning** (Trivy/Grype in rebuild.sh) | No CVE check for images handling PII data. Warn on HIGH, block on CRITICAL. | Medium | ✅ Yes | Wave 3 / `20-image-vuln-scanning` (after #5) | Security / Privacy Lead | Technical Implementation Lead |
| 21 | **Registry authentication gap** — document as known gap | Unauthenticated pushes accepted — undocumented, not a deliberate decision | Medium | ✅ Yes | Wave 4 / `21-registry-auth-gap` | Engineering Practices Lead | Security / Privacy Lead |
| 22 | **BP-001** SBOM auto-regen via cdxgen in rebuild.sh | Manual SBOM goes stale on every rebuild | Low | ✅ Yes (gated: Security Lead commits BP-001 → `approved-for-implementation`) | Wave 4 / `22-sbom-cdxgen` | Technical Implementation Lead | Security / Privacy Lead |
| 23 | **BP-003** bom.json serialNumber generation | Static UUID means two different SBOMs are indistinguishable | Low | ⛔ No — depends on #22 (cdxgen generates this) | Wave 4 (after #22) | Security / Privacy Lead | Technical Implementation Lead |
| 24 | **BP-008** Synthetic test corpus coverage gate | No quality gate for test corpus when new entity types are added | Low | ✅ Yes (surge role required) | Activate Detection/Data Researcher | Detection / Data Researcher (activate surge) | Engineering Practices Lead |
| 25 | **Cilium CNI** (DEC-004 Phase E) | Flannel enforces NetworkPolicy but not L7 identity — Cilium is Phase 3 target | Low | ⛔ No — Phase 3 scope + cluster disruption; plan required | Phase 3 | Platform / Cluster Infrastructure Lead (reactivate burst) | Security / Privacy Lead |
| 26 | **Prometheus → Worker direct scraping** | kubectl exec workaround — needs NetworkPolicy exemption rule | Low | ✅ Yes | Wave 4 / `26-prometheus-worker-scraping` | Technical Implementation Lead | Security / Privacy Lead |
| 27 | **Loki + Promtail** centralized log aggregation | Container logs not aggregated — kubectl logs only; Grafana already supports Loki | Low | ✅ Yes (additive, no breaking changes) | Activate Platform Lead burst | Platform / Cluster Infrastructure Lead (reactivate burst) | Engineering Practices Lead |
| 28 | **Image signing** (Cosign) | Images are unsigned — no integrity guarantee | Low | ✅ Yes | Wave 4 / `28-image-signing` | Security / Privacy Lead | Technical Implementation Lead |
| 29 | **Token revocation** (RFC 7009) | Compromised tokens valid for full TTL — no revocation endpoint | Low | ⛔ No — auth flow changes, Keycloak config, client impact | Future (Integration/API Lead) | Integration / API Lead (future — not yet active) | Security / Privacy Lead |
| 30 | **Dynamic Client Registration** (RFC 7591) | Clients must be pre-configured out of band — not discoverable | Low | ⛔ No — Keycloak + client registration endpoint work | Future (Integration/API Lead) | Integration / API Lead (future — not yet active) | Security / Privacy Lead |
| 31 | **Auth Code + PKCE** (interactive user path) | Only M2M supported — no interactive user auth flow | Low | ⛔ No — major feature, depends on #30 | Future (Integration/API Lead) | Integration / API Lead (future — not yet active) | Security / Privacy Lead |
| 32 | **Credential gate `--strict` review** | Tighten `check-credentials.sh` from warn-only to `--strict` after operational baseline confirmed | Low | ✅ Yes | Review after 1 month operational data | Security / Privacy Lead | Engineering Practices Lead |
| 33 | **Audit trail schema versioning** for template additions | `planning/vertical-templates/findings.md` open question: does adding `template_version` to scan responses require a schema version bump on the audit trail? No versioning mechanism exists today — consumers that read audit records will break silently if the schema changes. Define a schema version field and forward-compatibility policy before template implementation begins. | Medium | ⛔ No — depends on vertical templates phase proposal (#18) reaching council approval | Wave TBD (pre-template implementation) — raised in council review 2026-03-26 | Product / Scope Lead | Technical Implementation Lead |
| 34 | **bom.json PURL version mismatches** — two known errors in current file | `typing-extensions@4.15.0` has PURL `pkg:pypi/typing-extensions@0.15.0` (wrong: 4 → 0); `tqdm@4.67.3` has PURL `pkg:pypi/tqdm@4.67.0` (wrong patch). These are incorrect identifiers in the SBOM — a vulnerability scanner keyed on PURL will check the wrong version. Fix as part of branch `4-sbom-refresh` or as a standalone follow-up. | High | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Security / Privacy Lead | Technical Implementation Lead |
| 35 | **bom.json missing `annotated-doc` package** | `annotated-doc==0.0.4` appears in both `src/mcp_server/requirements.lock.txt` and `src/worker/requirements.lock.txt` but is absent from `bom.json`. The SBOM acceptance criterion for branch `4-sbom-refresh` requires every pinned lock file package to have a component entry — this package must be added. Fix as part of branch `4-sbom-refresh`. | High | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Security / Privacy Lead | Technical Implementation Lead |
| 36 | **Fix wrong client ID and misleading secret source in `7-rfc9728-integration-test.md`** | Step 5 uses `client_id=mcp-agent` but the Keycloak realm (`keycloak/realm-import/mcp-local-realm.json`) only registers `clientId: test-agent-client` — the test will get a 401 from Keycloak. Also, the instruction says to read the client secret from `values.local.yaml` but neither values.local.yaml contains the secret; it is hardcoded as `test-agent-secret-change-in-prod` in `scripts/auth-test.sh`, `scripts/demo.sh`, `scripts/classify.sh`, and the realm JSON. Both errors must be corrected in the instruction before implementation. | High | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Technical Implementation Lead | Security / Privacy Lead |
| 37 | **Stale `kind` comments in `helm/mcp-server/values.local.yaml`** | Header comment still says "Docker Desktop / kind / WSL2" and the NodePort comment references `kind-config.yaml`. Both are stale post-DEC-004 k3d migration. Low risk but will confuse the next agent. | Low | ✅ Yes | Wave TBD — raised in branch instruction review 2026-03-27 | Engineering Practices Lead | Technical Implementation Lead |
| 38 | **`setup-local.sh` uses fixed image tag `0.1.0`; no SHA-based tag path** | `setup-local.sh` hardcodes `presidio-worker:0.1.0` and `mcp-presidio-sensitivity:0.1.0` as local image names, while `rebuild.sh` uses a git-SHA tag. On a clean teardown+setup the SHA from a prior `rebuild.sh` run may not match what `setup-local.sh` expects. Document the relationship clearly (or align the tag strategy) to avoid "image not found" errors on first-time setup after partial builds. | Medium | ✅ Yes | Wave TBD — raised in branch instruction review 2026-03-27 | Technical Implementation Lead | Engineering Practices Lead |
| 47 | **Registry GC: `--delete-untagged` flag not specified in branch instructions** | `registry garbage-collect` defaults to preserving untagged manifests — it only removes orphaned blob layers with no referencing manifest. The accumulation problem this branch solves is old SHA-tagged manifests (every `rebuild.sh` run pushes a new tag). Those tags remain valid manifests; GC without `--delete-untagged` will not collect them. The `registry-gc.sh` script must document the `--delete-untagged` flag and its tradeoff: it reclaims the old SHA layers but will break any pod currently running a superseded SHA image if its layers are swept. The safe pattern is: run GC only after confirming all running pods reference the current image SHA. Branch `1-2-31-registry-gc` instructions need a correction before implementation. | High | ✅ Yes | Branch `1-2-31-registry-gc` — correction needed before implementation | Technical Implementation Lead | Security / Privacy Lead |
| 48 | **Registry GC: "safe to run while cluster is live" claim is unqualified** | The branch instructions state the script is "safe to run while the cluster is live." This is true for the marking phase but the sweep phase can corrupt data if a concurrent `docker push` races with blob deletion — a blob referenced by the in-flight push but not yet manifested can be deleted. The Docker Distribution docs recommend pausing writes (put registry in read-only mode) during GC. The qualification missing from the instructions is: GC is safe when no concurrent `docker push` to the registry is in progress. For this dev registry the risk is low but the instructions should state this explicitly, and the script should warn the operator. Branch `1-2-31-registry-gc` instructions need this qualification added. | Medium | ✅ Yes | Branch `1-2-31-registry-gc` — correction needed before implementation | Technical Implementation Lead | Security / Privacy Lead |
| 40 | **`setup-local.sh` does not reinstall Istio or apply `infrastructure/istio/*.yaml` after cluster rebuild** | After `setup-local.sh --teardown` + re-run, the Istio control plane is gone and the four Istio manifests (RequestAuthentication, AuthorizationPolicy, two EnvoyFilters, PeerAuthentication) are not reapplied. `status.sh` will fail RFC 9728 compliance checks and auth enforcement is absent until manifests are manually reapplied. No documented procedure exists for the Istio reinstall + manifest re-apply step. The DR runbook (branch `8-dr-runbook`) exposed this gap. Resolution: document the full post-rebuild Istio restore procedure and optionally add it to `setup-local.sh` as an idempotent step. | High | ✅ Yes | Wave TBD — raised in council review of branch `8-dr-runbook` 2026-03-26 | Technical Implementation Lead | Security / Privacy Lead |
| 41 | **Audit logs are container-ephemeral — not documented as a known data loss boundary** | The audit trail in `src/mcp_server/audit/trail.py` writes to stdout via Python logging. Container logs are available via `kubectl logs` only while the pod exists. On `setup-local.sh --teardown` or a WSL2 restart that destroys the cluster, all audit records are lost. This is expected for a local dev stack but is undocumented — a developer relying on audit records for a debugging session may run teardown not knowing records will be permanently lost. The DR runbook must state this explicitly. Backlog item #27 (Loki + Promtail) would resolve the underlying gap for Phase 3+. | Low | ✅ Yes (documentation only) | Wave TBD — raised in council review of branch `8-dr-runbook` 2026-03-26 | Engineering Practices Lead | Security / Privacy Lead |
| 39 | **Availability SLO is unmeasurable without a heartbeat metric or synthetic probe** | Council review of `18-19-planning` identified that no uptime/availability metric exists in the MCP server. `mcp_build_info` is set at startup but is not a health signal. An availability SLO (e.g. 99.5% uptime over a rolling 30-day window) requires either: (a) a heartbeat counter incremented by a periodic probe, or (b) an external synthetic probe (e.g. Blackbox Exporter hitting `/health`) scraped by Prometheus. Without one of these, the SLO definition produced by branch `18-19-planning` must mark availability as `[PLACEHOLDER — metric not yet instrumented]`. This item tracks adding the required instrumentation before Phase 3 SLO targets are finalized. | Medium | ✅ Yes (additive — new metric or probe only) | Wave TBD — raised in council review 2026-03-26 | Technical Implementation Lead | Engineering Practices Lead |
| 42 | **Audit log export / external audit sink — parity gap not documented** | Locally the audit trail writes to stdout with no external sink. In production an external audit sink (SIEM, log shipper, durable store) would be required for compliance. This is distinct from #41 (which documents ephemeral loss on teardown) and #27 (Loki/Promtail general log aggregation). The specific gap is: there is no production-equivalent durable audit sink in the local stack, so audit completeness under sustained operation, log rotation, and pod cycling cannot be tested locally. Document as a named parity gap in `planning/dev-prod-parity.md` (branch #11). Implementation of an actual external audit sink is Phase 3 scope. — raised in council review of branch `11-parity-delta` 2026-03-27. | Medium | ⛔ No — parity documentation depends on #11; implementation is Phase 3 scope | Parity doc: branch #11. Implementation: Phase 3. | Product / Scope Lead | Security / Privacy Lead |
| 43 | **`10-k3d-version-check` instruction uses `grep -oP` which fails in Alpine/BusyBox** | The branch instruction's version-parse snippet uses `grep -oP` (Perl regex). BusyBox `grep` in Alpine 3.20 (`devtools.Dockerfile` base image) does not support `-P` — confirmed by test. Since `setup-local.sh` runs inside the devtools container via `devtools-run.sh` during `branch-test.sh`, the version check will crash at the grep step and fail the branch gate. Fix: replace `grep -oP 'v\K[0-9]+\.[0-9]+\.[0-9]+'` with `grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' | sed 's/^v//'`. The `sort -V` comparison logic in the instruction is correct — BusyBox sort on Alpine 3.20 supports `-V`. The instruction must be corrected before any agent begins implementation of branch `10-k3d-version-check`. | High | ✅ Yes | Wave TBD — raised in Engineering Practices Lead branch instruction review 2026-03-27 | Engineering Practices Lead | Technical Implementation Lead |
| 44 | **`6-credential-gate` scan scope missing `infrastructure/keycloak-local.yaml` and `helm/keycloak-values.local.yaml`** | `check-credentials.sh` as specified in the branch-6 instruction scans only `helm/mcp-server/values.local.yaml`, `helm/presidio-worker/values.local.yaml`, and `keycloak/`. Two additional files contain hardcoded production-risk credentials outside this scope: `infrastructure/keycloak-local.yaml` sets `KC_BOOTSTRAP_ADMIN_PASSWORD: change-me-in-prod` in a Kubernetes Deployment env var; `helm/keycloak-values.local.yaml` sets `adminPassword: change-me-in-prod`. Both files would pass the gate undetected as written. The branch-6 instruction must be corrected to include these paths before implementation. | High | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Security / Privacy Lead | Engineering Practices Lead |
| 45 | **Audit and justify `pyjwt` remaining in `mcp_server/requirements.lock.txt` post-Phase 2** | JWT validation moved to Envoy (DEC-003). `PyJWT[crypto]` is still explicitly declared in `src/mcp_server/requirements.txt` (line 15) with the comment "JWT validation — PyJWT with cryptography backend for RS256 (Keycloak)". No `import jwt` appears in any `.py` source file under `src/mcp_server/`. If the library is genuinely unused, the explicit direct-dep pin should be removed to reduce cryptographic attack surface. If it is a required transitive dep of the MCP SDK, the comment must be corrected and the rationale for retaining the direct pin documented. Resolve before `4-sbom-refresh` merges so the SBOM does not list misleading components. | Medium | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Security / Privacy Lead | Technical Implementation Lead |
| 49 | **Worker `/tmp` tmpfs `sizeLimit: 128Mi` not validated against actual spaCy scratch usage** | The 128Mi limit on the worker `/tmp` emptyDir was set without documented rationale against actual Presidio/spaCy scratch usage. ENOSPC is a silent crash mode — it does not appear as `OOMKilled` in `kubectl describe` and will not be caught by memory monitoring. Should be measured during the #3 investigation and documented before Phase 3 adds sidecar memory pressure. | Medium | ✅ Yes | Wave TBD — raised in council review of branch `3-worker-restart-investigation` 2026-03-27 | Technical Implementation Lead | Engineering Practices Lead |
| 50 | **No standard for reviewing crash logs for PII before including in findings** | Investigation branches instruct agents to include `kubectl logs --previous` output in findings addenda. No standard exists for reviewing that output for payload content before committing or sharing. A Python traceback from Presidio's analyzer during a failed scan can include the text payload. Define a log-safety review step for all investigation branches. | Low | ✅ Yes | Wave TBD — raised in council review of branch `3-worker-restart-investigation` 2026-03-27 | Security / Privacy Lead | Engineering Practices Lead |
| 46 | **`bom.json` mcp-server component description is stale post-Phase 2** | The `mcp-server` component `description` field reads "FastAPI + FastMCP, JWT auth middleware, audit trail, OTel tracing, Prometheus metrics." The phrase "JWT auth middleware" is factually incorrect — auth moved to the Envoy sidecar (DEC-003). An auditor or automated tool reading the SBOM gets a false picture of where JWT validation occurs, which is a compliance documentation gap. Fix as part of `4-sbom-refresh`. | Low | ✅ Yes | Wave TBD — raised in council review 2026-03-27 | Security / Privacy Lead | Technical Implementation Lead |

---

## Completed since audit

| Item | Completed |
|------|-----------|
| branch-test.sh failures fixed (rebuild.sh worker health + validate-networkpolicy.sh timing) | 2026-03-27 |
| mTLS MCP→Worker via PeerAuthentication STRICT (DEC-001 resolved) | 2026-03-26 |
| Branch isolation: SHA-based image tags in rebuild.sh | 2026-03-26 |
| Agent workflow: branch-test.sh + flock + devtools integration | 2026-03-26 |
| DEC-005 logged in decision-log.md | 2026-03-26 |

---

## How to work an item

1. Pick an `✅ Yes` item
2. Create a branch: `git checkout -b <item-number>-short-description`
3. Implement and commit with clear messages
4. Run: `./scripts/devtools-run.sh ./scripts/branch-test.sh`
5. All 5 steps must pass before opening a PR
6. Tag the implementation owner and validation owner in the PR description
