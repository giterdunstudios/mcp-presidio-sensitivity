apiVersion: v1
kind: Service
metadata:
  name: {{ include "presidio-worker.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "presidio-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presidio-worker.selectorLabels" . | nindent 4 }}
